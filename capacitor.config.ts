import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'guias-remision-v3',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
